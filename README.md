#include<stdio.h>
#include<conio.h>
int main()
{
char c;
printf("enter the chracter");
scanf("%c",&c);
if((ch>='a'&&c<='z')
printf("%c is alphabet",ch);
else
printf("%c is not alphabet",ch);
return 0;
}
